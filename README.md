if you are using wsl run these:
python3 -m venv venv
source venv/bin/activate

to install the packages:
pip install -r requirements.txt

to generate/update dependencies:
pip freeze > requirements.txt
